#include<stdio.h>
#include<stdlib.h>
#define max 30
int stack[max];
int top=-1;
void push(int value)
{
	if(top<max-1)
	{
		stack[++top]=value;
	}
}
void print_binary(int num,int number)
{
	
	if(number>=0)
	{
		while(top>=0)
		{
			printf("%d",stack[top--]);
		}
	}
	else
	{
		printf("-");
		while(top>=0)
		{
			printf("%d",stack[top--]);
		}
	}
}
void decimal_to_binary(int num,int number)
{
	while(num!=0)
	{
		num=abs(num);
		push(num%2);
		num/=2;
	}
	
	
	printf("Binary:");
	if(top==-1)
	{	
		printf("0");
	}
	else
	{
		print_binary(num,number);
	}
	printf("\n");
}
int main()
{
	int num,ch;
	do
	{
		printf("Enter the decimal number:");
		scanf("%d",&num);
		int number=num;
		decimal_to_binary(num,number);
		printf("DO you want to continue?(0/1)");
		scanf("%d",&ch);
	}while(ch==1);
	return 0;
}











